# Email provider: "mailpit" (dev) or "resend" (prod)
EMAIL_PROVIDER=mailpit

# Mailpit SMTP (default for local dev)
MAILPIT_HOST=localhost
MAILPIT_PORT=1025

# Resend (production)
# RESEND_API_KEY=re_xxxxxxxxx
# RESEND_FROM=Invoice <invoice@yourdomain.com>

# Server
PORT=3001
